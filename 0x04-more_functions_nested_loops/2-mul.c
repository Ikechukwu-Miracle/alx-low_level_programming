#include "main.h"
/**
 * mul - gives the multiple of two numbers
 * @a: parameter of the function
 * @b: second parameter
 * Return: value of mul1
 */

int mul(int a, int b)
{
	int mul1;

	mul1 = a * b;

	return (mul1);
}
